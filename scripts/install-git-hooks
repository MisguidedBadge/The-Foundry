#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${ROOT_DIR}"

git config --local core.hooksPath .githooks

echo "Configured local git hooks path: $(git config --local --get core.hooksPath)"
