#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${ROOT_DIR}"

echo "pre-push: running scripts/verify-live"

if ! scripts/verify-live < /dev/null; then
  echo "pre-push: verification failed; blocking push" >&2
  exit 1
fi
